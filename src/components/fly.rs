pub struct Fly {
    pub max_speed:       f32,
    pub speed:           f32,
    pub detection_range: f32,

    pub attack_damage:   f32,
    pub attack_speed:    f32,
    pub attack_range:    f32,
}
